# Compiler files
cache/
out/

# Forge broadcast artifacts: ~90% raw RLP transaction blobs, and run-latest.json
# duplicates the timestamped run byte-for-byte. The only durable value is the
# deployed address per contract, which is recorded in CONTRACTS_PROD_DEPLOYMENT.md.
/broadcast/

# Docs
docs/

# Dotenv file
# .env

node_modules
.env.testnet